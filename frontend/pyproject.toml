[project]
name = "advanced-rag-frontend"
version = "0.1.0"
description = "Streamlit frontend for the RAG system (chat + slide panel)"
requires-python = ">=3.12,<3.14"
dependencies = [
    "streamlit>=1.40",
    "httpx>=0.27",
]
