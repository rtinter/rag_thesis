[project]
name = "rag-frontend-thesis"
version = "0.1.0"
description = "Streamlit frontend for the RAG system"
requires-python = ">=3.12,<3.14"
dependencies = [
    "streamlit==1.60.0",
    "httpx>=0.27",
]
